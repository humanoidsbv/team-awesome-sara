import * as Styled from '../styles/ProfileButton.styled';

const ProfileButton = () => {
  return (
    <Styled.ProfileButton>
        <img/>
    </Styled.ProfileButton>
  )
}

export default ProfileButton;
